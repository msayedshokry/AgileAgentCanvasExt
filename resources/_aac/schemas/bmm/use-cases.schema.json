{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://bmad.dev/schemas/bmm/use-cases.schema.json",
  "title": "Use Cases Collection",
  "description": "Per-epic collection of use cases stored in a standalone file (epics/epic-N/use-cases.json). Contains an array of use case objects plus metadata linking to the parent epic.",
  "type": "object",
  "required": ["metadata", "content"],
  "properties": {
    "metadata": {
      "$ref": "../common/metadata.schema.json",
      "description": "Standard BMAD artifact metadata. artifactType MUST be 'use-cases'. Must include epicId."
    },
    "content": {
      "type": "object",
      "required": ["useCases"],
      "properties": {
        "useCases": {
          "type": "array",
          "description": "Array of use case objects for this epic",
          "items": {
            "$ref": "use-case.schema.json"
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
}
